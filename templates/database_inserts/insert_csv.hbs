-- insert data
{{#each documents}}
INSERT INTO {{this.table_name}} (
    {{#each this.columns}} 
        {{this.name}}
        {{#unless @last}},{{/unless}} 
    {{/each}}
    ) VALUES
    {{#each this.rows}}
    (
        {{#each this.columns}}
            {{this.column}}
            {{#unless @last}},{{/unless}} 
        {{/each}}
    )
    {{#unless @last}},{{/unless}} 
    {{/each}}
);
{{/each}}